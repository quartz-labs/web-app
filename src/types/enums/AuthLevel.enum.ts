export enum AuthLevel {
    BASE = "Base",
    KYC_PENDING = "KYCPending",
    CARD = "Card",
    ADMIN = "Admin",
}

export enum TandCsNeeded {
    NOT_NEEDED = "NotNeeded",
    ACCEPTED = "Accepted",
    REJECTED = "Rejected",
}