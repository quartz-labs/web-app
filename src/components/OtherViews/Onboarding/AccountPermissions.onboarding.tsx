import type { OnboardingPageProps } from "./Onboarding";

export default function AccountPermissions({
    incrementPage, 
    decrementPage 
}: OnboardingPageProps) {
    return (
        <div>
            <h1>Personal Info</h1>
        </div>
    );
}